# Metadata generation

## Purpose

Build demo, scanner, and clinical tables from local sources.

## Entry points

gen_penn_metadata.py, gen_hcpaging_metadata.py

## Inputs

data/metadata

## Outputs

derivatives/metadata

## Configuration

Paths are resolved via [`config.yaml`](../config.example.yaml) and [`lib/paths.py`](../lib/paths.py).
