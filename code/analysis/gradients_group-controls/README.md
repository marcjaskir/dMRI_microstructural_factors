# Control gradients

## Purpose

BrainSpace diffusion map / LE gradients on control factor scores.

## Entry points

compute_gradients.py

## Inputs

factor_z-scores, atlas_centroids

## Outputs

derivatives/analysis/gradients_group-controls

## Configuration

Paths are resolved via [`config.yaml`](../../config.example.yaml) and [`lib/paths.py`](../../lib/paths.py).
